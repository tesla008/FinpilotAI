// Placeholder — rebuilt to match design/FinPilot AI Advisor.dc.html next,
// per the page-by-page build order (Landing → Dashboard → Import →
// Forecast → Advisor → What-If → Scam).
export function AdvicePage() {
  return (
    <div>
      <h1 className="font-heading text-h2 font-bold text-heading">AI Advisor</h1>
      <p className="mt-2 text-secondary">Rebuild coming next.</p>
    </div>
  )
}
