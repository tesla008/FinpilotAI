VITE_API_BASE_URL=http://localhost:8000

# Same value as the backend's GOOGLE_CLIENT_ID — this one is safe to expose,
# Google Identity Services' whole model relies on the client ID being public
# (the backend verifies the token's audience against it server-side).
VITE_GOOGLE_CLIENT_ID=
