[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Client-side routing (React Router) — let index.html handle every path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
